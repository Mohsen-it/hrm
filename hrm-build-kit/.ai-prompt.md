# 🚀 استخدم هذا النص عندما تفتح Cursor / Windsurf / أي AI كودينغ

انسخ والصق هذا الكود في الـ AI عندما تبدأ جلسة جديدة:

---

## 1️⃣ البداية السريعة (لجلسة جديدة)

```
Before making ANY changes, read these files in order:
1. .specify/memory/constitution.md  ← القوانين الإلزامية (أعلى سلطة)
2. AGENTS.md                         ← دليل الـ AI
3. specs/00-hrm-system/DESIGN.md     ← 🎨 نظام التصميم (ألوان، مكونات، RTL)
4. specs/00-hrm-system/spec.md       ← المواصفات الكاملة
5. specs/00-hrm-system/plan.md       ← خطة البناء
6. specs/00-hrm-system/tasks.md      ← قائمة المهام

You are building an HRM Laravel 13 SPA app:
- Framework: Laravel 13 + Inertia.js 3 + Vue 3 (Composition API) + Tailwind CSS 4.3
- Architecture: Controller → Service → Repository → Model
- UI: 100% SPA (no Blade views except app.blade.php)
- Language: Arabic (RTL) — كل شيء من اليمين لليسار
- Modules: 22 modules using nwidart/laravel-modules
- Reusable components only: DataTable, FormInput, FormModal, etc.

CRITICAL RULES:
1. N+1 queries are FORBIDDEN — always use with()/load()
2. NEVER write <table> manually — use <DataTable /> component
3. NEVER write <input> directly — use <FormInput /> component
4. NEVER write custom modals — use <FormModal /> component
5. NEVER use app()/resolve() in Services — use DI only
6. NEVER put business logic in Controllers — Services only
7. ALL tables must be text-right + dir="rtl"
8. ALL forms must have labels on the right + RTL inputs
9. Composition API only — no Options API in new components
10. Cache all queries, paginate all lists, eager load all relations

Start with the FIRST PENDING task from tasks.md and execute it completely.
After completing each task, verify with php artisan test if possible.
```

## 2️⃣ أثناء العمل (إذا توقف أو انحرف)

```
Read your last completed task from tasks.md.
State which task number you are on right now.
Read .specify/memory/constitution.md Article VII and XIV.
Ensure you are using shared components, not building from scratch.
```

## 3️⃣ كيف يقسم المهام (إذا كان السؤال عاماً)

```
Read C:\Users\pc\Desktop\hrm\specs\00-hrm-system\tasks.md
Pick the FIRST task that is NOT completed.
Understand its dependencies.
Read the module spec if needed from specs/{module}/spec.md
Execute ONE task at a time and wait for confirmation.
```

## 4️⃣ قائمة سريعة للتحقق (Checklist)

| ماذا | ممنوع | المسموح |
|------|-------|----------|
| جداول | `<table>` يدوي | `<DataTable />` |
| حقول | `<input>` مباشر | `<FormInput />` |
| نوافذ | `<div>` مخصص | `<FormModal />` |
| قوائم | `<select>` مباشر | `<FormSelect />` |
| استعلامات | N+1, DB::raw | `with()`, `chunk()`, `Cache::tags()` |
| منطق | في Controller | في Service |
| اعتماديات | `app()`, `resolve()` | Constructor DI |
| كود Vue | Options API | Composition API `setup` |
|اتجاه | LTR | RTL (`dir="rtl"`, `text-right`) |

## 5️⃣ هيكل المهمة الواحدة

عند تنفيذ أي Task من tasks.md:

```
1. READ: ملفات التبعية للمهمة
2. PLAN: الملفات التي ستنشئها/تعدلها
3. IMPLEMENT: اكتب الكود
4. VERIFY: php artisan test (إذا أمكن)
5. NEXT: أشر لأي مهمة تبدأ بعدها
```
