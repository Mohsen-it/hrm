# دليل P1-H — دقة التقارير 100% (فحص + إصلاح)

**التاريخ:** 2026-09-09

## 1. لغز إخفاق `6v7` (كان يفشل شهوراً) — محلول
**العرض:** `test_monthly_report_agrees_with_daily_breakdown` يفشل (6 بدل 7).

**التشخيص (بالتحقيق لا التخمين):**
- عمود `attendance_date` من نوع DATE، والموديل يحوّله بتاريخ (`date` cast)
  الذي يحفظ بصيغة `'Y-m-d H:i:s'`.
- MySQL (الإنتاج) يقطع الوقت صامتاً → `'2026-08-09'` → المقارنات دقيقة.
- SQLite (الاختبارات) بلا DATE حقيقي فيبقى `'2026-08-09 00:00:00'` كنص →
  `betweenDates(..., '2026-08-09')` تقارن نصياً: `'2026-08-09 00:00:00' >
  '2026-08-09'` → **اليوم الأخير يُستبعد دائماً** في بيئة الاختبار فقط.
- مثبت بتفريغ الصفوف الخام + فحص الإنتاج (`raw="2026-06-02" len=10`).

**الإصلاح (سطر واحد بـ scope):** الحد الأعلى صار `$to.' 23:59:59'`.
- على MySQL: يُقطع للتاريخ نفسه → **مجموعة النتائج مطابقة حرفياً**
  (مثبت: نفس md5 لثلاثة تقارير حية قبل/بعد).
- على SQLite: يشمل اليوم الأخير → الاختبار أخضر (49 assertion).
- الفهرس المركب ما زال مستخدماً (مقارنة مباشرة بلا `whereDate`).

## 2. فحص المطابقة المتقاطعة الحية (3 محركات × بيانات إنتاج)
نفس (الموظف، اليوم) عبر: الغياب الذكي اليومي ↔ التقرير اليومي
(`DailyReportService::build` — مصدر DOCX) ↔ الشهري — ليوم مكتمل (أمس):
- عينة 30: **0 اختلاف** ✅
- عينة 150: **0 اختلاف** ✅

## 3. الاختبارات بعد الإصلاح
- `AbsenceCalculationServiceTest`: **20/20** (كانت 19/20) ✅
- `tests/Unit/Modules/{Attendance,Shifts}`: **68/68** ✅
- تقارير الحضور (Daily/Summary/Session): **40/40** ✅
- pint على الموديل ✅. ملف الاختبار أُعيد كما كان (لا بقايا تشخيص).

## الخلاصة
تقارير الإنتاج كانت دقيقة أصلاً (عمود DATE) — والآن بيئة الاختبار تثبت
ذلك بدل أن تكذّبه. لا سلوك إنتاجي تغيّر (مثبت بالـ md5).
