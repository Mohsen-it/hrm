# سجل التقدم والإنجاز والمتبقي — المواصفة 011

**آخر تحديث:** 2026-09-09 — بدء المرحلة P0
**القاعدة:** كل بند يُغلق فقط بعد تحقق موثق. لا حذف، لا كسر.

---

## ✅ المنجز (موثق بالفحص — كان موجوداً قبل 011)

| # | البند | الدليل | التاريخ |
|---|---|---|---|
| 1 | Laravel daily log rotation يعمل | `laravel-2026-09-*.log` + `attendance-push-*.log` يومياً | موجود |
| 2 | `LOG_STACK=daily` + `LOG_LEVEL=warning` | `.env:18-22` | موجود |
| 3 | `CACHE_STORE=file` + `SESSION_DRIVER=file` (خارج database) | `.env:31,41` | موجود |
| 4 | `Cache::tags` guard مع fallback آمن | `Setting.php:117-136`، `ZoneService.php:182-196` | موجود |
| 5 | Queue `retry_after=610` + `after_commit=true` | `.env:52-56`، `config/queue.php:38-45` | موجود |
| 6 | Job `tries=3/backoff=10/timeout=600` متسق مع retry | `AttendanceIngestionJob.php:24-28` | موجود |

## 🔄 قيد التنفيذ (P0 — هذه المواصفة)

| # | البند | الملفات | الحالة | التحقق |
|---|---|---|---|---|
| P0-1 | تدوير stdout logs عند الإقلاع (أرشفة ≥50MB، بدون حذف) | `scripts/Start-HRM-Windows.ps1` | ✅ تم 2026-09-09 | PSParser syntax: 0 أخطاء |
| P0-2 | أرشفة 19 سكربت debug عبر `git mv` | الجذر → `backups/debug-archive-2026-09-09/` | ✅ تم 2026-09-09 | 19 renamed (R) + لا مرجع مكسور (grep) |
| P0-3 | فك تعارض مسار قناة `hrm_server` | `config/logging.php:142-148` | ✅ تم 2026-09-09 | `php -l` + pint ✅ + tests 32/32 ✅ |
| P0-4 | تصحيح سطر التوثيق (rotation ✅ غير دقيق) | `docs/OPTIMIZATION_ROADMAP.md:99` | ✅ تم 2026-09-09 | مراجعة نصية |
| P0-5 | إسقاط stdout لخدمة Laravel (الاحتفاظ بـ stderr فقط) — من 17MB/يوم إلى كيلوبايتات | `scripts/Start-HRM-Windows.ps1` (مفتاح `DiscardStdout`) | ✅ تم 2026-09-09 | PSParser 0 أخطاء + اختبار redirection حي (stdout يُتجاهل، stderr يُحفظ) |
| P0-6 | حذف تلقائي لأرشيفات stdout الأقدم من 14 يوم (بعد إثبات أنها 100% ضجيج) | `scripts/Start-HRM-Windows.ps1` (retention) | ✅ تم 2026-09-09 | PSParser 0 أخطاء + اختبار حي على ملفات وهمية (القديم يُحذف، الحديث والملف الحي يُحفظان) |
| P0-7 | حماية التشغيل الطويل: (أ) إضافة `app.py` لقائمة cleanup لمنع تعارض البورت، (ب) تحويل bridge failure من fatal إلى warning | `scripts/Start-HRM-Windows.ps1` | ✅ تم 2026-09-09 | PSParser 0 أخطاء — خدمة اختيارية ما بقت تغرق النظام كله |

## ✅ المرحلة P1 — الفهارس (تحقق 2026-09-09، بدون تغيير schema)

| # | نتيجة التحقق | الدليل |
|---|---|---|
| P1-1 | كل migrations الـ 008 التسعة **منفذة** على الإنتاج (batch 2) | `migrate:status` — 11/11 Ran |
| P1-2 | الفهارس الستة الرئيسية **موجودة** فعلياً | `SHOW INDEX` — 6/6 PRESENT |
| P1-3 | US4 منفذ (`orderBy users.id` + `getActiveByCompany`) | `UserRepository.php:59,218` |
| P1-4 | `EXPLAIN` على الجدول الكبير يستخدم الفهارس (`range`, 16 صف بدل 27k) | `att_sessions` — key مستخدم ✅ |
| P1-5 | `EXPLAIN users` يعطي `ALL` — **سلوك صحيح** لا مشكلة: 969/990 بنفس الشركة (المحسن يختار المسح الكامل وهو أسرع) | COUNT + توزيع البيانات |
| P1-6 | `IndexingTest`: **5/5 ناجحة** | `php artisan test --filter=IndexingTest` |
| P1-7 | **اكتشاف للمدى الطويل:** فهارس زائدة متداخلة على `attendance_sessions` (مثال: `att_sessions_user_date_idx` بادئة زائدة من `idx_att_sessions_user_date_status`) — تبطئ الكتابة وتستهلك قرصاً، **لم تُمس** بانتظار خطوة مخصصة بدليل قبل/بعد | `SHOW INDEX` — ~10 أشجار فهرس على جدول ينمو يومياً |

## ⏳ المتبقي (مراحل لاحقة — بموافقة صريحة)

| # | البند | المرحلة | ملاحظة أمان |
|---|---|---|---|
| R-1 | ~~إزالة الفهارس الزائدة على `attendance_sessions`~~ ✅ تم 2026-09-09 — migration `2026_09_09_000001` (حذف فهرسين بحماية covering-guard، تراجع مُختبر، EXPLAIN مطابق، tests خضراء) — الدليل: `evidence/p1a-index-dedup.md` | P1-تتمة | مغلق |
| R-2 | إصلاح N+1 في `AbsenceCalculationService` + كنترولرات Shifts | P1 | نفس الواجهة، نفس النتائج — اختبار مقارنة قبل/بعد |
| R-3 | مراجعة `queue:work --timeout=180/60` مقابل job timeouts | P1 | مراقبة فقط أولاً — لا تغيير أثناء التشغيل الحي |
| R-4 | توحيد مودال `FaceSyncDashboard` + زرّين خام + `ui/image.png` | P2 | تغيير واجهة فقط |
| R-5 | ملف صلاحيات مركزي + نواقص `AttendanceIntegration` | P2 | صلاحيات جديدة فقط — لا حذف/إعادة تسمية |
| R-6 | تصميم الإجازات 009 (جداول موازية) | ميزة جديدة | لا ترحيل للجداول القديمة |
| R-7 | أرشفة `hrm-laravel-server.log` الحالي (239MB) | عند إيقاف مخطط | **ممنوع أثناء التشغيل الحي** — فقط عند صيانة مجدولة |

## 📝 سجل الأحداث

| التاريخ | الحدث |
|---|---|
| 2026-09-09 | إنشاء المواصفة 011 + بدء P0 (بطلب المستخدم، وضع build) |
| 2026-09-09 | إنجاز P0 كاملاً: تدوير لوجات + أرشفة 19 سكربت + فك تعارض القناة + تصحيح توثيق — تحقق: pint ✅، tests 32/32 ✅، PSParser 0 أخطاء ✅ |
| 2026-09-09 | قياس نمو اللوج: 242MB/14.4 يوم (~17MB/يوم، إسقاط سنتين ≈ 12GB) — السبب: سطر stdout لكل request من polling الأجهزة |
| 2026-09-09 | إنجاز P0-5 (بطلب المستخدم): مفتاح `DiscardStdout` لخدمة Laravel — الأخطاء محفوظة مرتين (stderr + قناة laravel اليومية) |
| 2026-09-09 | **قاعدة دائمة معتمدة (بطلب المستخدم): كل عمل من الآن وصاعداً يُصمم للتشغيل الطويل الأمد** — لا ملفات بلا حدود، لا صيانة تعتمد على إعادة التشغيل فقط، مراعاة قيود ويندوز (ملف مفتوح لا يُنقل) |
| 2026-09-09 | فحص `scripts/Start-HRM-Windows.bat`: غلاف رقيق يستدعي الـ ps1 فقط (`%*` تمرير مباشر) — **لا يحتاج أي تعديل**، يرث كل تحسينات P0-1/P0-5 تلقائياً |
| 2026-09-09 | فحص محتوى اللوج الضخم: **2,142,043 سطر، 0 سطر خطأ (100% ضجيج requests)** — قرار المستخدم: الأرشيف بلا فائدة → سياسة حذف تلقائي بعد 14 يوم (P0-6) |
| 2026-09-09 | تشغيل بعد إعادة الإقلاع: **فشل Bridge** (القديم ما انحذف من cleanup → ماسك البورت 5000 + `throw` قتل كل الخدمات) → إصلاح فوري: إضافة `app.py` للcleanup + تحويل bridge failure إلى warning (P0-7) |
| 2026-09-09 | **تحقق ما بعد الإقلاع: نظامي ✅** — الأرشيف `hrm-laravel-server-20260909-193808.log` (242.75MB محفوظ) + اللوج الجديد **0 بايت** (P0-5 يعمل: من 17MB/يوم إلى صفر) + المنافذ 8000/8080/8081 مستمعة |
| 2026-09-09 | **P1-C منجز (تصحيح تشخيص سابق):** `getaddrinfo failed` ليس DNS شبكي — بل مسافة زائدة من `set VAR=x &` في cmd جعلت HOST=`"0.0.0.0 "` → Werkzeug لا يربط أبداً (مُعاد إنتاجه). الإصلاح: quoted SET + `.strip()` دفاعي + توسيع التدوير لـ bridge.log — الإثبات: BIND-FAILED→BIND-OK، والـ Bridge صحي الآن (`/health 200`) — الدليل: `evidence/p1c-bridge-bind-fix.md` |
| 2026-09-09 | **تحقق ما بعد إعادة الإقلاع الكامل: سليم ✅** — المنافذ الأربعة بعمليات جديدة (8000/8080/8081/5000)، الـ Bridge ربط ذاتياً من أول محاولة (إثبات P1-C بالإنتاج)، laravel `/login` 200، `adms-launcher.log` أُرشف تلقائياً (85MB→0)، server log ما زال 0 بايت، لا migrations معلقة، العدادات سليمة (users=990)، الطابور يُصرّف (0 معلق)، الـ 66 مهمة فاشلة كلها من 09-03 (مسبقة، صفر بآخر 24h) |
| 2026-09-09 | **P1-D منجز:** تدوير `adms.log` + `VACUUM` تلقائي للـ outbox بنافذة preflight (التشخيص: 0 صفوف لكن 99.8% من الملف فارغ) — الإثبات على نسخة: 28.15MB→0.02MB، والحي يُعالج عند الإقلاع القادم — الدليل: `evidence/p1d-adms-logs-outbox.md` |
| 2026-09-09 | **تحقق P1-D بالإنتاج الحي ✅:** سطر `Vacuumed ADMS outbox (now 0.02 MB)` ظهر بالإقلاع، الملف الحي 0.02MB والبنية سليمة (`command_cache`=5)، المنافذ الأربعة بعمليات جديدة (5000 ربط ذاتياً مجدداً)، laravel 200 + bridge 200 + مسار ADMS يرد — صفر تأثير على النظام |
| 2026-09-09 | **P1-A منجز:** حذف فهرسين زائدين من `attendance_sessions` (الفهارس كانت 3x حجم البيانات) — migrate بزمن 116ms والنظام الحي يكتب أثناءها بدون توقف، تراجع وإعادة مُختبران، EXPLAIN مطابق قبل/بعد، 48/48 اختبار حضور خضراء |
| 2026-09-09 | **P1-B منجز:** إصلاح N+1 للعطل (memoize per-instance + نسخة واحدة + تجميع punches) — إثبات حي: استعلامان→1 ونتائج مطابقة (15/260)، `AbsenceCalculationServiceTest` 19/20 (الوحيد مسبق ومثبت بالـ stash) — الدليل: `evidence/p1b-nplus1-holidays.md` |
| 2026-09-09 | **P1-E منجز (تعميق ب):** تجميع حلقة `getMonthlyAbsence` (~120 استعلام/موظف/شهر → 4) بنفس أنماط التقرير المُثبتة — إثبات حي: 16→3 استعلامات وmd5 مطابق للمخرج، suite 19/20 (نفس الإخفاق المسبق) — الدليل: `evidence/p1e-batch-month.md` |
| 2026-09-09 | **P1-F منجز (تقرير الغياب الذكي):** التقرير نفسه مُجمّع أصلاً — الـ N+1 الوحيدة كانت `breaks` الكسولة لكل assignment → سطر eager واحد. إثبات حي: 4→2 استعلامات وmd5 مطابق، pint ✅ — الدليل: `evidence/p1f-eager-breaks.md` |
| 2026-09-09 | **P1-G منجز:** تجميع `getEmployeeMonthlyAttendance` (إسنادات + بصمات: ~90/شهر → 3) — إثبات حي: 39→3 وmd5 مطابق. حادثة موثقة: تعارض اسم method اكتُشف بسكربت الإثبات قبل النشر وأُصلح — الدليل: `evidence/p1g-batch-empmonth.md` |
| 2026-09-09 | **P1-H منجز (دقة التقارير 100%):** حل لغز `6v7` (قطع sqlite لليوم الأخير — الإنتاج سليم أصلاً) بإصلاح scope سطر واحد، ومطابقة متقاطعة حية 150/150 بلا اختلاف بين الغياب الذكي/اليومي/DOCX/الشهري — الدليل: `evidence/p1h-report-accuracy.md` |
| 2026-09-09 | **P2-PERM منجز:** 13 صلاحية كانت ترفض الجميع (403) أُنشئت + إصلاح `sync→additive` (كان سيجرد super-admin عند إعادة التشغيل). الإثبات الحي: 104→117 صلاحية، باقي الأدوار 17=17، tests 16/16 — الدليل: `evidence/p2a-permissions-unify.md` |
| 2026-09-09 | **P2-UI منجز:** مودال FaceSyncDashboard → FormModal + زر خام → IconButton + نقل image.png غير المستخدمة. البناء ينجح، صفر باكند — الدليل: `evidence/p2b-ui-polish.md` |
| 2026-09-09 | **SERVER-1 (إقلاع ذاتي):** مفتاح `-NonInteractive` + غلاف `production/start-production.ps1` (يعمل داخل نفس العملية حتى لا يتيتم الـ stack) + نسخة احتياطية للمهمة + XML جاهز (SYSTEM/عند الإقلاع/مخفي/إعادة محاولة) + ملف `Register-Headless-Startup.bat` للتشغيل كمسؤول |
| 2026-09-09 | **اختبار حي كشف مشكلة بيئة حقيقية (والنظام سليم):** التشغيل المخفي أجهض بصوت عالٍ قبل أي تغيير لأن عمليات الـ stack الحالية **مرتفعة الصلاحيات** وجلسّتي غير مرتفعة لا تقتلها — كل المنافذ والخدمات بقيت كما هي (صفر توقف). الحل البنيوي: التسجيل كـ SYSTEM (الملف الجاهز) يوحد الصلاحيات للأبد |
| 2026-09-10 | **تصحيح ذاتي (تدقيق ما بعد الإقلاع):** مهمة SYSTEM أُطلقت عند الإقلاع (12:07:18) وفشلت exit 1 دون لوج — ليست طبقة حماية (one-shot أصلاً). الـ stack الحي أطلقه المشرف مباشرة (PID 12528 بـ -SkipBuild من جهة إقلاع قديمة). اكتشاف: C:\nssm حزمة إنتاج سابقة كاملة + ملف stdout قديم 242MB بحاجة أرشفة. الإجراء المطلوب (بصلاحيات مسؤول): تعطيل HRM-Startup + تفعيل سجل المهام التشغيلي |
| 2026-09-10 | **المشغّل المجهول = HRM-AutoStart.vbs بمجلد Startup (يعمل عند الدخول) + ملف HRM-Production.lnk يشير لغلافنا (نافذة مرئية + مكرر).** صلّبت الـ VBS بإضافة `-NonInteractive`. **حادثة مني:** أمر تحقق نفّذ الـ VBS وأطلق مشرفاً ثانياً — قتلته فوراً، والمشرف الأصلي أعاد بناء الـ stack ذاتياً (~دقيقة) — وهذا بحد ذاته إثبات حي أن حلقة المراقبة تعمل. الحالة الآن: مشرف واحد (12528)، غلاف واحد لكل خدمة، /login=200، صفر تكرار |
| 2026-09-10 | **تثبيت SERVER-1 نهائياً:** مهمة SYSTEM معطلة (Disabled مؤكد) + سجل المهام التشغيلي مفعّل + الـ lnk المكرر محذوف + ملف 242MB مؤرشف (اللوجات الحية 46MB) + وسيط `-ServerIp` باكتشاف LAN ذكي (RFC1918 — يتخطى VPN، ويختار 10.10.250.2 هنا تلقائياً). **الجزء 2 منجز:** `production/` فيها `.env.production.example` (70 مفتاحاً، قيم مُفرغة) + `Setup-NewMachine.ps1` (وضع `-CheckOnly` مُختبر) + `DEPLOY-NEW-MACHINE.md` + إصلاح `install-deps.bat` (مسارات نسبية + `python -m pip` بعد اكتشاف أن `pip.exe` مكسور والـ venv يعمل) |
| 2026-09-10 | **حل لغز exit-1 + تصليب الإقلاع:** شرطة `—` داخل string بقوسين بملفات ps1 تكسر محلل PS 5.1 (خروج 1 بلا لوج — وهذا ما قتل مهمة SYSTEM عند الإقلاع). طُهرت الملفات الثلاثة بالكامل (0 أخطاء بالمحلل الحقيقي ParseFile — ملاحظة: PSParser القديم لا يكشفها). أُضيفت إعادة محاولة فحص DB (6×10ث) ضد سباق MySQL-عند-الدخول. الوثيقة تتضمن القاعدة الدائمة |
| 2026-09-09 | **إصلاح وميض PowerShell:** مهمة `HRM Queue Worker Watchdog` (كل 5 دقائق) كانت تفتح نافذة مرئية — أُضيف `-WindowStyle Hidden` لأمر المهمة فقط (السكربت نفسه لم يُمس)، وجُربت يدوياً (23:29 → OK). ملاحظة: مهمة `HRM-Startup` تشير لملف غير موجود (`production/start-production.ps1`) — تُسجل خطأ صامتاً عند كل دخول، بانتظار قرار (تعطيل/إصلاح) |
